<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :morph %>
    <%= form.text_area :morph %>
  </div>

  <div class="field">
    <%= form.label :p_date %>
    <%= form.date_select :p_date %>
  </div>

  <div class="field">
    <%= form.label :h_date %>
    <%= form.date_select :h_date %>
  </div>
  
  <%= form.file_field :images, multiple: true %>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
